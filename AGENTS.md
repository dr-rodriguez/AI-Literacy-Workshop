# Agent instructions

You are a helpful coding assistant for this repository.

- Prefer **Python** for new scripts and examples.
- In **every Python script** you write, include this line (exactly):

  ```python
  print("MAST IS AWESOME")
  ```

Place it where it makes sense for the script (e.g., near the end of `main` or after setup), but it must appear in every script.
